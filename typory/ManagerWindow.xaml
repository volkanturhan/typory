<Window x:Class="typory.ManagerWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:typory"
        xmlns:services="clr-namespace:typory.Services"
        mc:Ignorable="d"
        Title="{Binding [ManagerTitle], Source={x:Static services:Localization.Instance}}"
        Width="540" Height="440"
        WindowStyle="None"
        ResizeMode="NoResize"
        AllowsTransparency="True"
        Background="Transparent"
        WindowStartupLocation="CenterScreen"
        FontFamily="Segoe UI">

    <Window.Resources>
        <!-- Small borderless "close" button shown in the custom title bar. -->
        <Style x:Key="CloseButtonStyle" TargetType="Button">
            <Setter Property="Width" Value="24" />
            <Setter Property="Height" Value="24" />
            <Setter Property="Foreground" Value="{DynamicResource TextMuted}" />
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="FontSize" Value="13" />
            <Setter Property="Margin" Value="8,0,0,0" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Bd" CornerRadius="5" Background="{TemplateBinding Background}">
                            <TextBlock Text="✕" HorizontalAlignment="Center" VerticalAlignment="Center"
                                       Foreground="{TemplateBinding Foreground}"
                                       FontSize="{TemplateBinding FontSize}" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="{DynamicResource Subtle}" />
                                <Setter Property="Foreground" Value="{DynamicResource TextPrimary}" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Flat rounded buttons. The accent background is supplied per-button so
             the secondary (Remove) button can override it; hover/disabled shades
             follow the active theme. -->
        <Style x:Key="ToolButton" TargetType="Button">
            <Setter Property="Foreground" Value="White" />
            <Setter Property="Background" Value="{DynamicResource Accent}" />
            <Setter Property="FontSize" Value="13" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="MinWidth" Value="84" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Bd" CornerRadius="7"
                                Background="{TemplateBinding Background}"
                                Padding="14,7">
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="{DynamicResource AccentHover}" />
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="Bd" Property="Background" Value="{DynamicResource Disabled}" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <!-- Outer margin leaves room for the drop shadow; the card is the Border. -->
    <Border Margin="12" CornerRadius="12"
            Background="{DynamicResource Surface}"
            BorderBrush="{DynamicResource Border}" BorderThickness="1">
        <Border.Effect>
            <DropShadowEffect Color="#000000" Opacity="0.22" BlurRadius="20" ShadowDepth="0" />
        </Border.Effect>

        <Grid Margin="16">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

        <!-- Header doubles as the custom title bar: brand mark, toolbar buttons,
             and a close button. The whole row is draggable. -->
        <Grid Grid.Row="0" Margin="0,0,0,12" Background="Transparent"
              MouseLeftButtonDown="OnHeaderDrag">
            <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                <Border Width="16" Height="16" CornerRadius="4"
                        Background="{DynamicResource Accent}" />
                <TextBlock Text="typory" Margin="9,0,0,0" FontWeight="SemiBold"
                           FontSize="15" Foreground="{DynamicResource TextPrimary}"
                           VerticalAlignment="Center" />
            </StackPanel>
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Center">
                <Button x:Name="AddButton" Style="{StaticResource ToolButton}"
                        Click="OnAdd"
                        Content="{Binding [Add], Source={x:Static services:Localization.Instance}}" />
                <Button x:Name="RemoveButton" Style="{StaticResource ToolButton}"
                        Margin="8,0,0,0" Background="{DynamicResource TextMuted}" Click="OnRemove"
                        Content="{Binding [Remove], Source={x:Static services:Localization.Instance}}" />
                <Button Style="{StaticResource CloseButtonStyle}" Click="OnCloseClick" />
            </StackPanel>
        </Grid>

        <!-- The editable list of snippets. -->
        <DataGrid x:Name="Grid" Grid.Row="1"
                  AutoGenerateColumns="False"
                  HeadersVisibility="Column"
                  GridLinesVisibility="Horizontal"
                  HorizontalGridLinesBrush="{DynamicResource GridLine}"
                  BorderBrush="{DynamicResource Border}" BorderThickness="1"
                  Background="{DynamicResource Surface}" RowBackground="{DynamicResource Surface}"
                  Foreground="{DynamicResource TextPrimary}"
                  CanUserAddRows="False" CanUserDeleteRows="False"
                  CanUserReorderColumns="False"
                  SelectionMode="Single" SelectionUnit="FullRow"
                  RowHeight="30" FontSize="13">
            <DataGrid.Resources>
                <!-- Theme the column headers and cells so text stays legible in
                     dark mode (the WPF defaults assume a light surface). -->
                <Style TargetType="DataGridColumnHeader">
                    <Setter Property="Background" Value="{DynamicResource Subtle}" />
                    <Setter Property="Foreground" Value="{DynamicResource TextPrimary}" />
                    <Setter Property="Padding" Value="8,4" />
                    <Setter Property="BorderBrush" Value="{DynamicResource Border}" />
                    <Setter Property="BorderThickness" Value="0,0,0,1" />
                </Style>
                <Style TargetType="DataGridCell">
                    <Setter Property="Foreground" Value="{DynamicResource TextPrimary}" />
                    <Setter Property="BorderBrush" Value="Transparent" />
                </Style>
            </DataGrid.Resources>
            <DataGrid.Columns>
                <DataGridTextColumn Width="160"
                                    Header="{Binding [ColAbbreviation], Source={x:Static services:Localization.Instance}}"
                                    Binding="{Binding Abbreviation, UpdateSourceTrigger=LostFocus}" />
                <DataGridTextColumn Width="*"
                                    Header="{Binding [ColExpansion], Source={x:Static services:Localization.Instance}}"
                                    Binding="{Binding Expansion, UpdateSourceTrigger=LostFocus}" />
            </DataGrid.Columns>
        </DataGrid>

        <!-- Hint. -->
        <TextBlock Grid.Row="2" Margin="2,12,2,0"
                   FontSize="12" Foreground="{DynamicResource TextMuted}"
                   TextWrapping="Wrap"
                   Text="{Binding [ManagerHint], Source={x:Static services:Localization.Instance}}" />
        </Grid>
    </Border>
</Window>
